let time = ["flamengo", "palmeiras", "bulls", "corinthias", "galo"];

for (let i = 0; i < time.length; i++) {
  oloco = time[i];
  console.log(`O ${i + 1}º time é: ${oloco}`);
}

console.log("---------------------------------------------------");

// atividade 2

let notas = [10, 9.5, 4, 5, 8, 7.5];
console.table(notas);
console.log(`index: ${time.length}`);
console.log(`quantidade de notas: ${time.length + 1}`);

for (let i = 0; i < notas.length; i++) {
  console.log(notas);
}

